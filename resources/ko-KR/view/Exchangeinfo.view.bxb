input-view {
 match {
    Exchangeinfo (this)
  }

  message (어느 기준화폐로 보시겠습니까?)

  render {
    selection-of (this) {
      where-each (item) {
        layout-macro (Exchangeinfo-summary-macro) {
          param (exchangeinfo) {
            expression (item)
          }
        }
      }
    }
  }
}